[package]
name = "hyperspace-roll"
version = "0.1.0"
authors = [""]
edition = "2021"

[dependencies]
agb = { version = "0.20.3", path = "../../agb" }

[profile.dev]
opt-level = 3
debug = true

[profile.release]
opt-level = 3
lto = "fat"
debug = true
