{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Rust build: debug",
            "command": "cargo",
            "args": [
                "build"
            ],
            "options": {
                "env": {
                    "CARGO_TARGET_DIR": "${workspaceRoot}/target"
                }
            }
        },
    ],
}